# oracledb-resultset-stream

Simple wrapper around Oracle ResultSet to make it Streamable.

```npm install oracledb-resultset-stream```

Look into test file for usage example

```specs/OracleResultSetStreamSpecs.js```
